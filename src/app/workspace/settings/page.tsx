export default function Example() {
    return (
        <>
            <p className="text-sm text-gray-500">[/settings should not be publicly exposed, if clicked forward to /settings/general]</p>
        </>
    )
}